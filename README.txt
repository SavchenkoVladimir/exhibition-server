This is a template project.
It will be used as a template for a restfull app based on NodeJS.

To fire this application up execute 'npm start' at app-root directory. The working app will be 
available by 'http://localhost:3000/app/<action_name>' address.

This app works with MongoDB database. Its name is exhibition. You can find the DB backup
in dbBackups folder.